#!/usr/bin/env bash
# Zero-install launcher for the Agentic CLI proof-of-concept.
# Runs without pip: no third-party dependencies required.
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec env PYTHONPATH="$DIR/src${PYTHONPATH:+:$PYTHONPATH}" python3 -m agentic "$@"
